version: '3.9'

services:
  stormcore:
    build: .
    container_name: stormcore
    ports:
      - "8000:8000"
    volumes:
      - ./:/app
    command: uvicorn api.server:app --host 0.0.0.0 --port 8000
